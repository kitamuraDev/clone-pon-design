<div id="hero">
  <div class="inner">
    <div class="heading">
      <h2 class="title">WEB DESIGN SPECIALIST</h2>
      <p class="sub-title">お客様の夢を叶える<br class="sp-only-br" />Webサイトを制作</p>
    </div>
    <div class="contact-link">
      <a href="/contact">CONTACT</a>
    </div>
  </div>
  <span class="hero-scroll">SCROLL</span>
</div>

<style lang="scss">
  @import '../../../scss/main';

  #hero {
    height: 100svh;
    display: flex;
    justify-content: center;
    align-items: center;
    color: #fff;
    background-image: url(/home/hero.jpg);
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center center;
  }

  .inner {
    text-align: center;
    padding: 0 30px;

    @include mq(md) {
      text-align: left;
    }

    .heading .title {
      font-size: rem(72);
      line-height: 1;
      letter-spacing: 0.01em;

      @include mq(md) {
        font-size: rem(40);
        line-height: 1.1;
      }
    }
    .heading .sub-title {
      font-size: rem(24);
      letter-spacing: 0.1em;
      line-height: 1.8;
      margin-top: 20px;

      @include mq(md) {
        font-size: rem(16);
      }

      .sp-only-br {
        display: none;
        @include mq(md) {
          display: block;
        }
      }
    }
  }

  // ========================================
  // contact link
  // ========================================
  .contact-link {
    margin-top: 50px;

    @include mq(md) {
      margin-top: 30px;
    }

    a {
      text-decoration: none;
      position: relative;
      display: inline-block;
      text-align: center;
      width: 220px;
      padding: 18px 0;
      font-size: rem(14);
      font-weight: 700;
      line-height: 1.4;
      letter-spacing: 0.1em;
      color: inherit;
      border: 1px solid #fff;
      border-radius: 80px;
      transition: 0.3s;

      @include hover() {
        &:hover {
          opacity: 0.8;
        }
      }
    }

    a::after {
      content: '\f0da';
      position: absolute;
      top: 50%;
      right: 1.5em;
      font-family: 'Font Awesome 5 Free';
      font-weight: 900;
      transform: translateY(-50%);
      transition: 0.3s;
    }
    @include hover() {
      a:hover::after {
        right: 1.2em;
      }
    }
  }

  // ========================================
  // scroll down line
  // ========================================
  .hero-scroll {
    display: inline-block;
    position: absolute;
    right: 20px;
    bottom: 0;
    color: #fff;
    padding: 10px 10px 90px;
    font-size: rem(12);
    letter-spacing: 0.2em;
    line-height: 1;
    overflow: hidden;
    writing-mode: vertical-lr;
  }
  .hero-scroll::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 1px;
    height: 80px;
    background: #fff;
    animation: scrollDownLine 1.5s infinite;
  }

  @keyframes scrollDownLine {
    0% {
      transform: scale(1, 0);
      transform-origin: 0 0;
    }
    50% {
      transform: scale(1, 1);
      transform-origin: 0 0;
    }
    50.1% {
      transform: scale(1, 1);
      transform-origin: 0 100%;
    }
    100% {
      transform: scale(1, 0);
      transform-origin: 0 100%;
    }
  }
</style>
